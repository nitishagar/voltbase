# voltbase local env — NAMES ONLY, never values (IS-05 / docs/env.md).
# Copy to `.dev.vars` (gitignored) for local runs. Remote secrets are set via
# `wrangler secret` at S13 only. Missing key => typed UNCONFIGURED skip.
OCM_API_KEY=
CLOUDFLARE_API_TOKEN=
CLOUDFLARE_ACCOUNT_ID=
GITHUB_TOKEN=
