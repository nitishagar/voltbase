# ADR-001 — stack (S0 decision, 2026-09-14)
WHAT: Hono 4.13.7 default; itty-router 5.0.24 challenger rejected; plain Worker fallback; TS+npm toolchain pinned below. WHY: rubric margins + spikes; no challenger clears the ≥10pt + spike-pass bar.
Rubric (R1 `docs/research/frameworks.md` §1, max 114): Hono 105 / itty 100 / plain 79 / Rust 54 / Go 43 / Fastify 31 / NestJS 26.
Margins: Hono−itty = 5pt (<10, no overturn); Hono−plain = 26pt (plain no spike per brief, stays fallback); Hono−Rust = 51pt; Hono−Go = 62pt.
Spikes (reused `.spike/` code, D1 one-row + POST-redirect, observed 2026-09-14): itty 4295 raw / 1911 gz, 2/2 pass; hono 94991 raw / 23383 gz (JSX SSR), 2/2 pass. MCP-on-Workers baseline ≈298 KB gz (lumen dist; zod+mcp-server+ajv ≥81% — grouping-dependent, dominance holds).
npm over pnpm (W2): WHAT keep npm. WHY lumen parity (`package-lock.json` + npm workspaces, observed 2026-09-14) + fewer moving parts; spikes' `pnpm-lock` divergence noted, not followed.
Pins: TypeScript 5.9.3 exact (6.0.3 approved fallback), NOT 7.0.2 — typescript-eslint 8.70.0 peer `>=4.8.4 <6.1.0` (`npm view`, 2026-09-14); TS7 programmatic-API gap V-secondary, revisit at 7.1. vitest 4.1.11 + `@cloudflare/vitest-plugin` 1.1.9 (peer `vitest ^4.1.0`; successor of pool-workers; gotcha: `readD1Migrations` unexported → inline SQL via `define`). Validation/lint: zod 4.6.5 over valibot 1.5.0 (lumen parity; CF docs advise zod ≥4.5 for Worker memory; tree-shaken delta UNVERIFIED); eslint 10.10.0 + typescript-eslint over biome 2.5.13 (lumen-parity choice; lumen itself pins eslint 9 — frameworks.md §3). wrangler 4.131.2.
Go verdict: REJECTED for now. WHY Wasm-only experimental (`syumai/workers-go` README, observed 2026-09-14; TinyGo ≥0.42 for net/http), Go modules ≠ npm distribution, second toolchain for team-of-one; 43pt, no spike. Size not the reason (64 MiB uncompressed cap).
Rust verdict: REJECTED for now. WHY first-class (`workers-rs`, Env KV/R2/D1/Queues; docs observed 2026-09-14) but rmcp/tokio-on-Wasm compat UNVERIFIED + single-TS-toolchain cost; 54pt, no spike. Revisit either only on ≥10pt win + spike pass.
Fail list: Fastify 5.12.4 (Node http APIs absent in workerd, W1=0, no spike — rationale UNVERIFIED against primary source, frameworks.md §6); NestJS 12.0.2 (heavy DI tree vs isolate/1s-startup, W1=0, no spike — rationale UNVERIFIED, frameworks.md §6).
CSS: WHAT single hand-written stylesheet, no build step (PLAN §2 default). WHY spikes need nothing heavier (hono/jsx zero-CSS; itty raw HTML + htmx as static asset, not bundled); S0 proves no change.
